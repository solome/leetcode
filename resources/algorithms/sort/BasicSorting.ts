import { Comparable } from "@leetcode/shared-utils/Comparable"

/**
 * 基础排序：选择、插入、冒泡、希尔.
 */
export class BasicSorting {
  static select<T extends Comparable<T>>(list: T[]): T[] {
    return []
  }

  static insert<T extends Comparable<T>>(list: T[]): T[] {
    return []
  }

  static bubble<T extends Comparable<T>>(list: T[]): T[] {
    return []
  }

  static shell<T extends Comparable<T>>(list: T[]): T[] {
    return []
  }
}
